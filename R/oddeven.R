print("Enter the starting range")
a=scan(n=1)
print("Enter the closing range")
b=scan(n=1)
for (i in a:b) {
	if (i%%2==0) {
     		cat(i," is even number\n")
      } else {
		cat(i,"is odd number\n")
}
}