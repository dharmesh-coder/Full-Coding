s
q()
local({pkg <- select.list(sort(.packages(all.available = TRUE)),graphics=TRUE)
if(nchar(pkg)) library(pkg, character.only=TRUE)})
25 225 22 5
q()
q()
repeat {
   cat("Enter a no of choice -press 0 to terminate"
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
   while(x) {
   a=x%%10
   sum=sum+a
   paste(sum)
}
}
repeat {
   cat("Enter a no of choice -press 0 to terminate"
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
   while(x>0) {
   a=x%%10
   sum=sum+a
   x=x/10
   paste(sum)
}
}
repeat {
   cat("Enter a no of choice -press 0 to terminate"
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
   while(x>0) {
   a=x%%10
   sum=sum+a
   x=x/10
}
   cat(sum)
}
repeat {
   cat("Enter a no of choice -press 0 to terminate"
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
   while(x>0) {
   a=x%%10
   sum=sum+a
   x=x/10
   }
   cat(sum)
}
repeat {
   cat("Enter a no of choice -press 0 to terminate")
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
   while(x>0) {
   a=x%%10
   sum=sum+a
   x=x/10
   }
   cat(sum)
}
 repeat {    cat("Enter a no of choice -press 0 to terminate")
   x=scan(n=1)
   if (x==0) {
   break
   }
   sum=0
    while(x>0) {
    a=x%%10
    sum=sum+a
    x=x%/%10
    }
    cat(sum)
 }
q()
for (i in 1:10) {
  cat(2*i)
}
for (i in 1:10) {
  cat(2*i,"\n")
}
print("Enter the number")
n=scan(n=1)
for (i in 1:10) {
  cat(2*n,"\n")
}
print("Enter the number")
n=scan(n=1)
for (i in 1:10) {
  cat(2*n,"\n")
}
print("Enter the number")
x=scan(n=1)
for (i in 1:10) {
  cat(2*x,"\n")
}
print("Enter the number")
x=scan(n=1)
  cat(2*x,"\n")
}
print("Enter the number")
x=scan(n=1)
  cat(i*x,"\n")
}
for (i in 1:10) {
  cat(i*2,"\n")
}
x=scan(n=1)
  cat(i*x,"\n")
}
x=scan(n=1)
for (i in 1:10) {
  cat(i*x,"\n")
}
q()
print("Enter the number")
x=scan(n=1)
for (i in 1:10) {
  cat(i*x,"\n")
}
q()
my_vect <- c(2:20)
op <- rep(my_vect,l=10)
op
my_vect
op1 <- seq(1,10,(2**n)/n)
op1 <- seq(1,10,(2**by)/by)
op1 <- seq(1,10,by=**/)
q()
source("gcd.R")
gcd(10,25)
q()
